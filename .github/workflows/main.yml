name: CI

on: [push]

jobs:
  build-linux-stable:
    runs-on: ubuntu-latest
    env:
      TRAVIS_OS_NAME: linux
      ATOM_CHANNEL: stable
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Run Docker
      uses: thumperward/ci@feat/dockerfile
